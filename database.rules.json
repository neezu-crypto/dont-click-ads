{
  "rules": {

    "ads": {
      ".read":  true,
      ".write": false
    },

    "players": {
      ".read": true,
      "$uid": {
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['nickname','clicks','createdAt','updatedAt']) && newData.child('nickname').isString() && newData.child('nickname').val().length >= 1 && newData.child('nickname').val().length <= 16 && newData.child('clicks').isNumber() && newData.child('clicks').val() >= 0"
      }
    },

    "rankings": {
      ".read": true,
      ".indexOn": ["score"],
      "$uid": {
        ".write": "auth != null && auth.uid === $uid && (!data.exists() || newData.child('score').val() >= data.child('score').val())",
        ".validate": "newData.hasChildren(['nickname','score','totalTime','clearedAt']) && newData.child('score').isNumber() && newData.child('score').val() >= 0 && newData.child('score').val() <= 10000 && newData.child('totalTime').isNumber() && newData.child('totalTime').val() >= 0 && newData.child('nickname').isString() && newData.child('nickname').val().length >= 1 && newData.child('nickname').val().length <= 20"
      }
    },

    "adInquiries": {
      ".read": "auth != null && auth.token.email === 'skftodwocks2@gmail.com'",
      "$key": {
        ".write": "!data.exists()",
        ".validate": "newData.hasChildren(['nickname','soopId','days','totalStarBalloons','createdAt']) && newData.child('nickname').isString() && newData.child('nickname').val().length >= 1 && newData.child('nickname').val().length <= 20 && newData.child('soopId').isString() && newData.child('soopId').val().length >= 1 && newData.child('soopId').val().length <= 30 && newData.child('days').isNumber() && newData.child('days').val() >= 1 && newData.child('days').val() <= 30 && newData.child('totalStarBalloons').isNumber() && newData.child('createdAt').isNumber()"
      }
    },

    "quizStage": {
      "imageUrl": {
        ".read":  true,
        ".write": false
      },
      "answer": {
        ".read":  false,
        ".write": false
      },
      "updatedAt": {
        ".read":  false,
        ".write": false
      }
    },

    "$other": {
      ".read":  false,
      ".write": false
    }
  }
}
